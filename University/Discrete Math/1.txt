// дослідження рефлективності

#include<iostream> 
#include<string>  
using namespace std;

int main()
{
	setlocale(LC_ALL, "Ukrainian");
	while (true)
	{
		cout << "Показати функцiї - натиснiть 1" << endl;
		cout << "Вийти - натиснiть 2" << endl;

		int choice;
		cin >> choice;

		if (choice == 1)
		{
			cout << "Оберiть рiвнiсть:" << endl;
			cout << "1) 13x + 7y = 2000 " << endl;
			cout << "2) x^x + y^y >= 2012" << endl;
			cout << "3) 2 * x * y <= 2012" << endl;
			choice = 0;
			cin >> choice;
			switch (choice)
			{
			case 1:
				cout << "Задайте кiлькiсть елементiв:";
				int n = 0;
				cin >> n;
				int *size = new int[n];
				
				for (int i = 0; i < n; i++)
				{
					cout << "Задайте " << i + 1 << " значення: ";
					cin >> size[i];
				}

				break;
				/*			case 2:
				cout << "Задайте кількість елементів:";
				int n;
				cin >> n;
				int *size = new int[n];

				break;

				case 3:
				cout << "Задайте кількість елементів:";
				int n;
				cin >> n;
				int *size = new int[n];

				break;*/
				//			default:
				//		break;
			}
		}

		if (choice == 2)
		{
			break;
		}
	}
	system("pause");
}
